# 書籍レビューアプリ 要件定義書・基本設計書（基本機能編）- 生徒用

## 1. はじめに

### 1.1. 本書の目的と進め方

本書は、書籍レビューアプリケーションの「基本機能編」開発における**初期要件**を定義するものです。実際の開発現場では、最初から完璧な仕様書が渡されることは稀です。多くの場合、大まかな要件を元に、開発者は**自ら詳細な仕様を考え、設計し、クライアントやPMとコミュニケーションを取りながら**プロダクトを形にしていきます。

この教材は、そうした現場のプロセスを体験することを目的としています。

> **【この教材の設定】**
> 
> あなたは開発会社のエンジニアです。クライアント企業のPM（プロジェクトマネージャー）から、書籍レビューアプリの開発を依頼されました。PMからは大まかな要件と画面デザイン（Bladeテンプレート）が提供されていますが、詳細な仕様は固まっていません。
> 
> **コーチ = クライアントPM** として、あなたはPMに対してヒアリングを行い、詳細な仕様を詰めていく必要があります。

> **【この教材の進め方】**
> 1.  **要件の把握**: 本書に書かれている大まかな機能要件、URI、UI（Bladeテンプレート）を把握します。
> 2.  **思考と設計**: 「この機能を実現するには、どんなデータが必要か？」「どんなバリデーションが必要か？」「成功時/失敗時の画面遷移はどうすべきか？」といった**詳細な仕様（基本設計）を自分自身で考え、言語化**してください。
> 3.  **PMへのヒアリング**: 自分で考えた設計案をPM（コーチ）に提示し、「この仕様で問題ないか？」「他に考慮すべき点はあるか？」といった確認を行います。不明点があれば積極的に質問してください。
> 4.  **設計の確定**: PMからのフィードバックを元に設計をブラッシュアップし、最終的な仕様を確定させます。
> 5.  **実装**: 確定した設計を元に、実装に着手します。

本書には、あえて「正解」となる詳細な仕様は書かれていません。**要件の行間を読み、自ら思考し、PMとコミュニケーションを取って仕様を詰めていく**プロセスそのものが、この教材の最も重要な学習要素です。

### 1.2. ヒアリングのポイント

PMへのヒアリングでは、以下のような観点で質問・確認を行うと効果的です。

- **画面遷移**: 「登録成功後はどの画面に遷移しますか？」「エラー時はどうなりますか？」
- **バリデーション**: 「タイトルの最大文字数は何文字ですか？」「ISBNの形式チェックは必要ですか？」
- **エラーメッセージ**: 「バリデーションエラー時のメッセージはどのような文言にしますか？」
- **フラッシュメッセージ**: 「登録成功時に表示するメッセージは何ですか？」
- **権限・認可**: 「他人の書籍を編集しようとした場合、どのような挙動にしますか？」
- **エッジケース**: 「紐付く書籍があるジャンルを削除しようとした場合はどうしますか？」

### 1.3. 前提条件

- 開発言語・フレームワークは PHP / Laravel (Laravel 10.x) を使用する。
- フロントエンドのBladeテンプレートは `coachtech-material/Preparedblade-mockcase-BookShelf` リポジトリの `basic` ブランチで提供される。
- データベースは MySQL を使用する。
- 開発環境は Docker および Laravel Sail を使用する。

## 2. 環境構築手順

### 2.1. Laravelプロジェクトの作成 (Laravel 10.x)

以下のDockerコマンドを実行して、Laravel 10.xを明示的に指定してプロジェクトを作成します。

```bash
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    -e COMPOSER_CACHE_DIR=/tmp/composer_cache \
    laravelsail/php82-composer:latest \
    composer create-project laravel/laravel:^10.0 book-review-app
```

### 2.2. Laravel Sailのインストール

プロジェクト作成後、`book-review-app` ディレクトリに移動し、Laravel Sailをインストールします。

```bash
# プロジェクトディレクトリに移動
cd book-review-app

# Laravel Sailをインストール
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    -e COMPOSER_CACHE_DIR=/tmp/composer_cache \
    laravelsail/php82-composer:latest \
    composer require laravel/sail --dev

# Sailの設定ファイルをパブリッシュ（MySQLを選択）
docker run --rm \
    -u "$(id -u):$(id -g)" \
    -v "$(pwd):/var/www/html" \
    -w /var/www/html \
    -e COMPOSER_CACHE_DIR=/tmp/composer_cache \
    laravelsail/php82-composer:latest \
    php artisan sail:install --with=mysql
```

### 2.3. フロントエンドのセットアップ (Vite & Tailwind CSS)

1.  **NPM依存パッケージのインストール**
    ```bash
    sail npm install
    ```

2.  **Tailwind CSSのインストール**
    ```bash
    sail npm install -D tailwindcss@^3.4.0 postcss autoprefixer
    ```

3.  **設定ファイルの生成**
    ```bash
    sail npx tailwindcss init -p
    ```

4.  **Tailwind CSSのテンプレートパス設定**
    `tailwind.config.js` を開き、`content`プロパティを以下のように設定します。
    ```javascript
    /** @type {import('tailwindcss').Config} */
    export default {
      content: [
        "./resources/**/*.blade.php",
        "./resources/**/*.js",
        "./resources/**/*.vue",
      ],
      theme: {
        extend: {},
      },
      plugins: [],
    }
    ```

5.  **CSSファイルにTailwindディレクティブを追加**
    `resources/css/app.css` の中身を以下の3行に置き換えます。
    ```css
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    ```

6.  **Vite開発サーバーの起動**
    新しいターミナルを開いて実行します。このコマンドは開発中、常に実行したままにしてください。
    ```bash
    sail npm run dev
    ```

### 2.4. .env ファイルとphpMyAdminの設定

1.  **.env ファイルの確認**
    `.env` ファイルを開き、データベース接続情報が以下と一致していることを確認します。
    ```ini
    DB_CONNECTION=mysql
    DB_HOST=mysql
    DB_PORT=3306
    DB_DATABASE=laravel
    DB_USERNAME=sail
    DB_PASSWORD=password
    ```

2.  **phpMyAdminの追加**
    `compose.yaml` を開き、`mysql` サービスの後に以下の設定を追加します。
    ```yaml
        phpmyadmin:
            image: 'phpmyadmin:latest'
            ports:
                - '${FORWARD_PHPMYADMIN_PORT:-8080}:80'
            environment:
                PMA_HOST: mysql
                PMA_USER: '${DB_USERNAME}'
                PMA_PASSWORD: '${DB_PASSWORD}'
            networks:
                - sail
            depends_on:
                - mysql
    ```

### 2.5. Sailの起動とエイリアス設定

1.  **Sailの起動**
    ```bash
    ./vendor/bin/sail up -d
    ```

2.  **エイリアス設定**
    ```bash
    echo "alias sail='[ -f sail ] && bash sail || bash vendor/bin/sail'" >> ~/.zshrc
    # または bash の場合
    # echo "alias sail='[ -f sail ] && bash sail || bash vendor/bin/sail'" >> ~/.bashrc
    
    # シェルを再起動
    exec $SHELL
    ```

3.  **アプリケーションキーの生成**
    ```bash
    sail artisan key:generate
    ```

## 3. 機能要件

### 3.1. 機能一覧

| 大機能 | 中機能 | 機能概要 |
|---|---|---|
| **認証** | 会員登録 | メールアドレス、パスワード、名前でユーザー登録できる。 |
| | ログイン | メールアドレスとパスワードでログインできる。 |
| | ログアウト | ログイン状態からログアウトできる。 |
| **書籍管理** | 書籍一覧表示 | 登録されている書籍を10件ずつのページネーションで一覧表示する。 |
| | 書籍詳細表示 | 特定の書籍の詳細情報と、その書籍に紐付くレビューの一覧を表示する。 |
| | 書籍登録 | 書籍のタイトル、著者、ISBN、出版日、概要、画像URL、ジャンルを登録できる。 |
| | 書籍編集 | 自分が登録した書籍の情報を編集できる。 |
| | 書籍削除 | 自分が登録した書籍を削除できる。 |
| **レビュー管理** | レビュー投稿 | 書籍詳細ページから、5段階評価とコメントでレビューを投稿できる。 |
| | レビュー編集 | 自分が投稿したレビューを編集できる。 |
| | レビュー削除 | 自分が投稿したレビューを削除できる。 |
| **お気に入り機能** | お気に入り登録/解除 | 書籍をお気に入りに登録、またはお気に入りから解除できる。 |
| | お気に入り一覧 | 自分がお気に入りに登録した書籍を一覧表示できる。 |
| **いいね機能** | いいね登録/解除 | 他のユーザーのレビューに「いいね」を登録、または解除できる。 |
| **ジャンル管理** | ジャンル一覧表示 | 登録されているジャンルを、紐付く書籍数と共に一覧表示する。 |
| | ジャンル別書籍一覧 | 特定のジャンルに紐付く書籍を一覧表示する。 |
| | ジャンル登録 | 新しいジャンル名を登録できる。 |
| | ジャンル編集 | 既存のジャンル名を編集できる。 |
| | ジャンル削除 | 紐付く書籍がない場合に限り、ジャンルを削除できる。 |
| **ランキング機能** | ランキング表示 | レビューの平均評価が高い順に書籍トップ10をランキング形式で表示する。 |

### 3.2. 設計のヒント：バリデーションと認可

安全なアプリケーションを作るためには、**バリデーション（入力値検証）**と**認可（権限管理）**が不可欠です。以下の機能については、これらの実装が必要になることを念頭に置いて設計を進めてください。

- **バリデーションが必要な機能**:
  - 会員登録
  - ログイン
  - 書籍登録、編集
  - レビュー投稿、編集
  - ジャンル登録、編集

- **認可（ポリシー）が必要な機能**:
  - 書籍編集、削除（自分の書籍か？）
  - レビュー編集、削除（自分のレビューか？）

## 4. URI設計

| HTTPメソッド | URI | アクション | ルート名 |
|---|---|---|---|
| GET | `/` | 書籍一覧表示 | `books.index` |
| GET | `/books/create` | 書籍登録フォーム表示 | `books.create` |
| POST | `/books` | 書籍登録処理 | `books.store` |
| GET | `/books/{book}` | 書籍詳細表示 | `books.show` |
| GET | `/books/{book}/edit` | 書籍編集フォーム表示 | `books.edit` |
| PUT/PATCH | `/books/{book}` | 書籍更新処理 | `books.update` |
| DELETE | `/books/{book}` | 書籍削除処理 | `books.destroy` |
| POST | `/books/{book}/reviews` | レビュー投稿処理 | `reviews.store` |
| GET | `/reviews/{review}/edit` | レビュー編集フォーム表示 | `reviews.edit` |
| PUT/PATCH | `/reviews/{review}` | レビュー更新処理 | `reviews.update` |
| DELETE | `/reviews/{review}` | レビュー削除処理 | `reviews.destroy` |
| POST | `/favorites/{book}/toggle` | お気に入り登録/解除 | `favorites.toggle` |
| GET | `/favorites` | お気に入り一覧表示 | `favorites.index` |
| POST | `/reviews/{review}/like` | いいね登録/解除 | `reviews.like` |
| GET | `/genres` | ジャンル一覧表示 | `genres.index` |
| GET | `/genres/create` | ジャンル登録フォーム表示 | `genres.create` |
| POST | `/genres` | ジャンル登録処理 | `genres.store` |
| GET | `/genres/{genre}` | ジャンル別書籍一覧表示 | `genres.show` |
| GET | `/genres/{genre}/edit` | ジャンル編集フォーム表示 | `genres.edit` |
| PUT/PATCH | `/genres/{genre}` | ジャンル更新処理 | `genres.update` |
| DELETE | `/genres/{genre}` | ジャンル削除処理 | `genres.destroy` |
| GET | `/ranking` | ランキング表示 | `ranking.index` |

## 5. UI

UI（画面デザイン）は、`coachtech-material/Preparedblade-mockcase-BookShelf`リポジトリの`basic`ブランチで提供されるBladeテンプレートを使用してください。これらのテンプレートを見れば、各画面でどのようなフォーム項目が必要になるか、どのような情報を表示すべきかのヒントが得られます。

## 6. テスト

本アプリケーションでは、各機能が正しく動作することを保証するために、**フィーチャーテスト**を実装することを要件とします。自分で設計した仕様通りにアプリケーションが動作するか、テストコードを書いて検証してください。

---

## 7. あなたのタスク

上記の情報を元に、以下のステップで開発を進めてください。

### Step 1: 基本設計書の作成

各機能について、以下の項目を自分で考え、設計書としてまとめてください。

- **画面遷移**: 正常系・異常系それぞれの遷移先
- **フラッシュメッセージ**: 成功時・失敗時に表示するメッセージの文言
- **バリデーションルール**: 各入力項目に対するルール（必須、最大文字数、形式など）
- **エラーメッセージ**: バリデーションエラー時に表示するメッセージの文言
- **認可ルール**: 誰がどの操作を行えるか

### Step 2: PMへのヒアリング

作成した設計書をPM（コーチ）に提示し、以下を確認してください。

- 設計内容に問題がないか
- 考慮漏れがないか
- 不明点の解消

### Step 3: 実装

PMとの合意が取れた設計を元に、実装に着手してください。
